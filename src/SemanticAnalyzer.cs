using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Rythin.src
{
    /*class Symbol {
        public string Name = { get; }
        public string Value = { get; }

        public Symbol(string name, string value) {
            this.Value = value;
            this.Name = name;
        }
    }

    public class SymbolTable {
        private readonly Dictionary<string, Symbol> symbols = new Dictionary<string, symbol>();

        public void AddSymbol(Symbol sy) {
            if (sy.ContainsKey(sy.Name))
            {
                throw new Exception($"Error: Symbol '{sy.Name}' already defined!")
            }
            symbols[sy.Name] = sy;
        }

        public Symbol GetSymbol(string name) {
            if (!sy.ContainsKey(name)) {
                throw new Exception($"Symbol '{name}' is not defined yet!")
            }
            return symbols[name];
        }
    }*/
}